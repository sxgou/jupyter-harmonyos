[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "zmq-shim"
version = "4.3.5"
description = "Pure-Python ctypes ZMQ shim for HarmonyOS"
license = {text = "BSD"}
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
